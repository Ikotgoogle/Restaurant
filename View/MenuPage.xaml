<Page x:Class="Restaurant.View.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Restaurant.View"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="850"
      Title="MenuPage">

    <Grid Background="Beige">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Image Source="/images/lupa.png" HorizontalAlignment="Left"/>
            <TextBox Margin="13" Width="270" HorizontalAlignment="Right"/>

            <ListView Grid.Row="1" Margin="10" Background="AntiqueWhite">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID"/>
                        <GridViewColumn Header="Название"/>
                        <GridViewColumn Header="Тип"/>
                        <GridViewColumn Header="Цена"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="130"/>
                <RowDefinition/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="DishTitle" Text="" Margin="10" FontSize="50" FontWeight="DemiBold"/>
            <TextBlock x:Name="DishDesc" Text="" Margin="10" FontSize="18" FontWeight="DemiBold" Grid.Row="1" TextAlignment="Justify" TextWrapping="Wrap"/>

            <StackPanel Grid.Row="2" Margin="10">
                <DockPanel>
                    <Label Content="Цена: " FontSize="22" FontWeight="DemiBold"/>
                    <TextBlock Text="" VerticalAlignment="Center" FontSize="22" FontWeight="DemiBold"/>
                </DockPanel>
                <DockPanel>
                    <Label Content="Белки: " FontSize="16"/>
                    <TextBlock Text="" VerticalAlignment="Center" FontSize="16"/>
                </DockPanel>
                <DockPanel>
                    <Label Content="Жиры: " FontSize="16"/>
                    <TextBlock Text="" VerticalAlignment="Center" FontSize="16"/>
                </DockPanel>
                <DockPanel>
                    <Label Content="Углеводы: " FontSize="16"/>
                    <TextBlock Text="" VerticalAlignment="Center" FontSize="16"/>
                </DockPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
