<Window x:Class="Restaurant.View.Result"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.View"
        mc:Ignorable="d"
        Title="Result" Height="450" Width="800">
    
    <Grid Name="qwe">
        <StackPanel Name="buttonCommand" VerticalAlignment="Center">
            <Button x:Name="Cash" Width="220" Height="50" Margin="0,0,0,25" 
                    Content="Наличный" FontSize="30" FontWeight="Medium"
                    Command="{Binding SetUpPage}" CommandParameter="Cash"
                    />
            
            <Button x:Name="NonCash" Width="220" Height="50" 
                    Content="Безналичный" FontSize="30" FontWeight="Medium"
                    Command="{Binding SetUpPage}" CommandParameter="NonCash" 
                    />
            <ContentControl  Content="{Binding ContentCtrl, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Width="200" Height="100" Content="Рассчет" Click="makeResut"/>
        </StackPanel>
    </Grid>
</Window>
